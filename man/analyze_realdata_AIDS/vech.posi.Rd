% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyze_realdata_AIDS.R
\name{vech.posi}
\alias{vech.posi}
\title{Generate lower-triangular (including diagonal) index pairs for vectorizing a symmetric matrix}
\usage{
vech.posi(dim)
}
\arguments{
\item{dim}{integer(1). The dimension \eqn{q} of the covariance matrix \eqn{D}
(i.e., \eqn{D \in \mathbb{R}^{q \times q}}).}
}
\value{
An integer matrix with two columns, where each row is a lower-triangular
index pair \eqn{(i, j)} with \eqn{i \ge j}:
\describe{
\item{\link{,1}}{Row index \eqn{i}.}
\item{\link{,2}}{Column index \eqn{j}.}
}
}
\description{
For a \eqn{q \times q} symmetric matrix (e.g., the random-effects covariance matrix \eqn{D}),
return all index pairs \eqn{(i, j)} such that \eqn{i \ge j}, ordered by row.
This is useful for the \emph{vech} vectorization (lower triangle including the diagonal).
}
